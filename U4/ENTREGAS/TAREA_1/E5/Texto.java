package U4.E5;

public class Texto {/*
    public int num_vocal(){
        int vocal=0;

        for (int i = 0; i < cad.lenght() ; i++) {
            if (es_vocal(cad.charAt(i))){
                num_vocal++;
            }
        }
        return num_vocal;
    }
    
    private boolean es_vocal=false;
    c=Character.tolowerCase(c);
    if (vocales.indexOf(c)!=-1){
        es_vocal=true;
    }
    return es_vocal;
    */
}
